# Qwen営業ルート計画機能 仕様書（MVP）

- 文書版：2.1
- 対象リポジトリ：`ishiishiishii/AI_work_partner`
- 対象ブランチ：`main`
- 基準コミット：`b7265ca`
- 想定利用者：営業担当者
- 通貨：日本円
- 日時：日本標準時（Asia/Tokyo）

## 1. 目的

営業担当者が「明日の営業計画を作って」とQwenへ依頼すると、既存の顧客・商談・営業所・予定データを使い、移動効率だけでなく、売上予定額と粗利の両方を考慮した訪問ルート案を作成する。

Qwenは依頼の理解、専用処理の呼び出し、結果の説明を担当する。住所変換、金額計算、移動時間取得、訪問先選定、ルート最適化はFastAPI側の専用プログラムで実行する。

訪問先の組み合わせはOR-Tools CP-SAT、具体的な訪問順と時刻はOR-Tools RoutingModelで計算する。2つのソルバーを一度ずつ直列実行するだけではなく、CP-SATが作った複数の訪問先セットをRoutingModelで比較し、売上・粗利条件と移動効率を両立する案を採用する。

## 2. MVPの完成条件

次の一連の処理が動作すればMVP完成とする。

1. Qwenが「明日の営業計画を作る」という依頼を認識する。
2. 担当者、計画日、勤務時間、出発・帰着地点を確定する。
3. PostGISで担当エリア内の近隣候補を最大20社に絞る。
4. FastAPIが候補ごとの売上予定額、粗利、期待売上、期待粗利を計算する。
5. Routes APIで各地点間の移動時間と距離を取得する。
6. OR-Tools CP-SATが売上・粗利条件を満たす訪問先セットを上位10案作る。
7. OR-Tools RoutingModelが10案それぞれの訪問順、予定時刻、移動コストを計算する。
8. FastAPIが実行可能な案を比較し、最終案を決定する。
9. Qwenが選定理由、売上と粗利のバランス、見送り理由を説明する。
10. 営業担当者が承認した場合だけ、既存の `activity_plan` に保存する。

## 3. 現在利用できるデータ

GitHubの現行シードデータをMVPの入力として使用する。

| データ | 現在の状態 | MVPでの用途 |
|---|---|---|
| 顧客 | 300社。都道府県、市区町村、番地までの実在住所を `customer.location` に保持 | 座標化、近隣検索、訪問地点 |
| 営業担当者 | 50人。`sales_rep.branch_id` で所属営業所を保持 | 計画対象者、担当エリア判定 |
| 営業所 | 札幌、仙台、東京、中部、神戸、広島、九州の7拠点 | 出発・帰着地点、管轄判定 |
| 都道府県マスタ | 47都道府県を7営業所へ割り当て | 顧客所在地と担当営業所の整合確認 |
| 商談 | 5,000件 | 訪問候補、売上・粗利評価 |
| 商品 | 100件 | 商談内容の説明 |
| 売上目標 | 108件 | 目標進捗を考慮した優先度調整 |
| 活動予定 | `activity_plan` に訪問・作業予定を保持 | 固定予定、重複防止、承認後の保存先 |

### 3.1 商談で利用する既存項目

| 項目 | 意味 |
|---|---|
| `estimated_amount` | 売上予定額 |
| `cost` | 予定原価 |
| `win_probability` | 受注確率（0〜100） |
| `deal_phase_id` | 商談フェーズ |
| `deal_result_status_id` | 進行中、受注、失注 |
| `expected_visit_count` | 想定訪問回数 |
| `expected_effort_hours` | 商談全体の想定工数 |
| `deal_start_date` | 商談開始日 |
| `contract_date` | 契約日 |

現在の金額は基本的に予定値である。確定した実売上・実原価ではないため、画面とQwenの説明では「売上予定額」「予定粗利」「期待粗利」と表示し、「実売上」「実粗利」と表現しない。

新規商談登録時の `cost` は、現在のデモ実装では売上予定額の50〜95%から自動生成される。この値による粗利評価はMVP検証用であり、実運用前には実際の原価入力または商品原価との連携が必要である。

### 3.2 担当エリアの扱い

- 新規商談は、顧客住所の管轄営業所と担当者の所属営業所が一致することを登録時に検証済みとする。
- シード上の新規商談1,000件は、原則として所在地に合う営業所へ割り当てられている。
- 過去商談4,000件は地域一致が保証されないため、ルート候補抽出時に都道府県マスタを使って再検証する。
- 担当営業所が一致しない過去商談は、自動計画へ入れず「担当エリア外」として表示する。

## 4. MVPの対象範囲

### 4.1 実装する機能

- 翌日または指定日の1日ルート作成
- 1人の営業担当者に対する計画作成
- 自動Geocodingと結果保存
- 担当エリア・距離による候補抽出
- 売上予定額と予定粗利を両立する候補評価
- 固定アポイントと既存予定の考慮
- 車移動を前提とした移動時間・距離取得
- CP-SATによる売上・粗利条件を満たす訪問先セットの生成
- RoutingModelによる訪問順・予定時刻・移動コストの最適化
- Qwenによる自然言語説明
- ユーザー承認後の計画保存

### 4.2 MVPでは実装しない機能

- Qwen自身によるWeb検索、住所推測、金額計算、最短経路計算
- 確定売上・請求・会計原価の管理
- 複数営業担当者を同時に最適化する配車問題
- リアルタイムGPS追跡
- 移動中に見つけた未登録企業の自動登録
- 飛び込み訪問後の自動再計画
- Googleカレンダー連携
- アポイントの自動取得、電話、メール送信
- 天候・渋滞事故によるリアルタイム再計画

## 5. システム構成と責務

```text
営業担当者
  ↓「明日の計画を作って」
Qwen
  ↓ 構造化ツール呼び出し
FastAPI Plan Orchestrator
  ├─ Geocoding Service
  ├─ Candidate Service（PostGIS）
  ├─ Economics Service（売上・粗利計算）
  ├─ Route Matrix Service（Routes API）
  ├─ Portfolio Optimizer（OR-Tools CP-SAT）
  ├─ Route Optimizer（OR-Tools RoutingModel）
  └─ Plan Evaluator（複数案の比較）
  ↓ 計算済みの計画案
Qwenによる説明
  ↓ ユーザー承認
activity_planへ保存
```

| コンポーネント | 責務 |
|---|---|
| Qwen | 意図理解、必要情報の確認、ツール呼び出し、結果説明 |
| FastAPI | 認証、入力検証、各サービスの実行、結果保存、エラー制御 |
| Postgres/PostGIS | 顧客・商談・予定・座標の保存、近隣検索 |
| Geocoding API | 実在住所から緯度・経度を取得 |
| Routes API | 地点間の道路移動時間と距離を取得 |
| OR-Tools CP-SAT | 売上・粗利・訪問件数・顧客構成の条件を満たす訪問先セットを生成 |
| OR-Tools RoutingModel | Routes APIの時間行列を使い、訪問順・到着時刻・移動コストを最適化 |
| Plan Evaluator | RoutingModelで実行可能だった複数案を事業価値と移動効率で比較 |

Qwenには、内部処理をまとめた `create_sales_route_plan` ツールだけを公開する。Qwenから外部APIやDBを個別に自由操作させない。

## 6. 追加・変更するデータ

### 6.1 顧客の位置情報

既存の `customer` に次を追加する。

| 項目 | 型の例 | 内容 |
|---|---|---|
| `latitude` | numeric | 緯度 |
| `longitude` | numeric | 経度 |
| `geo_point` | geography(Point, 4326) | PostGIS検索用座標 |
| `place_id` | text、NULL可 | Geocoding APIの地点ID |
| `geocoding_status` | text | `pending` / `success` / `review` / `failed` |
| `geocode_accuracy` | text、NULL可 | 取得結果の精度 |
| `geocoded_at` | timestamptz、NULL可 | 最終変換日時 |

住所変更時は座標を無効化し、`geocoding_status = pending` に戻す。

### 6.2 営業所の位置情報

既存の `branch` に次を追加する。

| 項目 | 内容 |
|---|---|
| `location` | 営業所の実在住所 |
| `latitude` / `longitude` | 営業所の座標 |
| `geo_point` | PostGIS Point |

MVPでは、担当者は所属営業所を出発して同じ営業所へ戻る。個人の自宅や現在地は使用しない。

### 6.3 訪問条件

商談または訪問予定に次の情報を持たせる。

| 項目 | 初期値 | 内容 |
|---|---|---|
| `visit_duration_min` | 60分 | 1回の訪問滞在時間 |
| `visit_window_start` | NULL | 訪問可能時刻の開始 |
| `visit_window_end` | NULL | 訪問可能時刻の終了 |
| `must_visit` | false | 固定アポイントか |
| `visit_deadline` | NULL | 訪問期限 |

既存の `expected_effort_hours` は商談全体の工数なので、1回の訪問時間には直接使用しない。

### 6.4 ルート計画

承認前の案と承認済み計画を区別するため、ルート計画と立ち寄り地点を保存するテーブルを用意する。

最低限保存する項目：

- 担当者ID、対象日、出発・帰着営業所
- 計画状態：`proposed` / `approved` / `rejected` / `failed`
- 使用した重みと制約
- CP-SATが生成した訪問先セット数と各案の事業価値
- RoutingModelの探索方式、制限時間、求解状態
- 合計売上予定額、合計予定粗利
- 合計期待売上、合計期待粗利
- 合計移動時間、合計距離
- 訪問順、顧客ID、商談ID
- 到着時刻、出発時刻、滞在時間
- 区間移動時間、区間距離
- 採用理由、除外理由
- Qwenモデル、プロンプト版、作成日時

`activity_plan` への書き込みは承認時にのみ行う。

## 7. 売上・粗利の計算仕様

計算はFastAPIのEconomics Serviceで行い、Qwenには計算済みの値を渡す。

```text
売上予定額 = estimated_amount
予定原価 = cost
予定粗利 = estimated_amount - cost
予定粗利率 = 予定粗利 / estimated_amount × 100
期待売上 = estimated_amount × win_probability / 100
期待粗利 = 予定粗利 × win_probability / 100
時間当たり期待粗利 = 期待粗利 /（訪問時間 + 当該顧客を追加することで増える移動時間）
```

### 7.1 計算ルール

- 金額計算には浮動小数点ではなくDecimalまたは整数を使用する。
- `estimated_amount <= 0` の場合、粗利率は計算しない。
- `cost` がNULLの場合は粗利を0とせず「評価不可」にする。
- `cost > estimated_amount` の場合は赤字案件として負の粗利を保持する。
- 受注確率は0〜100の範囲外を入力エラーとする。
- 表示金額は円単位に丸める。

### 7.2 訪問価値スコア

候補間で金額規模が大きく異なるため、期待売上や期待粗利をそのまま足さず、候補集合内で0〜100へ正規化する。

```text
訪問価値スコア =
  売上評価 × sales_weight
  + 粗利評価 × gross_profit_weight
  + 緊急度 × urgency_weight
  + 商談フェーズ評価 × phase_weight
  + 売上目標不足度 × target_gap_weight
```

初期重み：

| 評価 | 重み |
|---|---:|
| 期待売上 | 25% |
| 期待粗利 | 35% |
| 緊急度 | 20% |
| 商談フェーズ | 10% |
| 売上目標不足度 | 10% |

粗利を売上より少し重くし、大きな売上だけで低採算案件が選ばれ続けることを防ぐ。重みは環境設定または管理テーブルで変更できるようにし、コードへ固定しない。

移動時間は訪問価値スコアとは別に、RoutingModelの移動コストと時間制約として評価する。表示時には、選択理由として「期待粗利」「期待売上」「追加移動時間」を並べる。

### 7.3 CP-SATによる訪問先セット生成

顧客ごとに訪問するかどうかを表す0/1変数を持ち、同じ顧客の複数商談は1つの訪問候補へ集約する。

CP-SATの主な制約：

- 固定アポイントの顧客は必ず選ぶ。
- 選択件数は `max_visits` 以下とする。
- 固定予定、訪問時間、概算移動時間の合計を勤務可能時間以下とする。
- `policy` に最低期待売上または最低期待粗利が設定されている場合、その値以上とする。
- 必要に応じて新規顧客数、既存顧客数、最低粗利率などの業務条件を設定する。

CP-SATの目的関数は訪問価値スコアの合計最大化とする。ただし1案だけを確定せず、採用済みの組み合わせを除外する制約を追加しながら再求解し、異なる上位10案を作る。

最低期待売上などの必須条件を満たす案が存在しない場合は、条件を黙って無視しない。`target_not_reachable` を返し、最低条件なしで作った代替案と不足額を表示する。

### 7.4 最終案の比較規則

CP-SATの上位案をRoutingModelで経路化した後、次の辞書式優先順位で最終案を決める。

1. 勤務時間、固定アポイントなどの必須制約をすべて満たす。
2. 設定された最低期待売上・最低期待粗利を満たす。
3. 合計期待粗利が高い。
4. 合計期待売上が高い。
5. 合計移動時間が短い。

同一条件内で重み付きスコアを使用してもよいが、必須条件違反を高い売上や粗利で相殺してはならない。

## 8. 処理フロー

### 8.1 Qwenによる依頼判定

対象例：

- 「明日の営業計画を作って」
- 「8月27日の訪問ルートを考えて」
- 「売上と粗利を両立するルートを作って」

Qwenが確定する入力：

- `rep_id`：ログインユーザーから取得
- `target_date`：相対表現を現在日付から確定
- `policy`：既定は `balanced`

対象日または担当者が不明な場合だけ確認する。住所、座標、金額はQwenに生成させない。

### 8.2 座標の準備

1. 初回導入時に300社の `location` を一括Geocodingする。
2. 企業名を検索語にせず、住所文字列だけをAPIへ送る。
3. 結果を `customer` に保存し、通常の計画作成では再取得しない。
4. 精度不足、複数候補、都道府県不一致は `review` とする。
5. `failed` または `review` の顧客は自動ルート候補から除外する。

Qwenのインターネットアクセスで住所を調査する方式は採用しない。

### 8.3 PostGISによる候補抽出

抽出条件：

1. `deal` が進行中である。
2. 顧客のGeocodingが `success` である。
3. 顧客の都道府県管轄と担当者の営業所が一致する。
4. 対象日に同じ商談の訪問予定が重複していない。
5. 担当営業所または固定アポイント周辺の検索半径内である。

処理：

1. 固定アポイントは必須候補として先に確保する。
2. その他を距離、期限、商談フェーズ、期待売上、期待粗利で仮順位付けする。
3. 最大20社に絞る。
4. 20社未満の場合だけ検索半径を段階的に広げる。

同じ顧客に複数の進行中商談がある場合、訪問先は1地点にまとめ、画面では対象商談を複数表示する。訪問価値は対象商談の合計を使う。

### 8.4 Routes APIによる移動時間取得

入力地点：

- 出発・帰着営業所
- 固定アポイント
- 候補顧客最大20社

地点間の道路移動時間と距離を行列として取得する。移動手段はMVPでは自動車に固定する。

- 同じ地点ペア、移動手段、時間帯の結果はキャッシュする。
- APIの最大要素数を超えないよう、候補数を設定値で制限する。
- 一部区間を取得できない顧客は除外して再計算する。
- 直線距離を確定移動時間の代用にしない。

### 8.5 CP-SATによる訪問先セット生成

Economics Serviceの計算値、固定アポイント、訪問可能時間、最大訪問数をCP-SATへ渡し、7.3の仕様に従って異なる上位10案を生成する。

CP-SATではRoutes APIの行列から作った概算移動時間も勤務時間上限の判定に使う。ただし訪問順を決めない段階の概算なので、CP-SATだけで計画を確定しない。

各案について次を出力する。

- 選択した顧客ID・商談ID
- 合計売上予定額、合計予定粗利
- 合計期待売上、合計期待粗利
- 訪問価値スコア
- 満たした条件と満たせなかった条件
- CP-SATの求解状態

### 8.6 RoutingModelによる経路最適化

CP-SATが生成した各訪問先セットについて、同じRoutes API時間・距離行列をRoutingModelへ渡して経路を計算する。

必須制約：

- 勤務時間内に出発・帰着する。
- 既存予定と時間を重複させない。
- 固定アポイントの時間帯を守る。
- 各訪問の滞在時間を確保する。
- 1日の最大訪問数を守る。
- 同一顧客を1日に複数回訪問しない。
- 出発・帰着地点は所属営業所とする。

RoutingModelの目的：

```text
移動時間・移動距離・待ち時間を最小化する。
```

初期解は `PARALLEL_CHEAPEST_INSERTION`、改善探索は `GUIDED_LOCAL_SEARCH` を既定とする。Time Dimensionで移動時間、訪問時間、待ち時間、時間枠を管理する。

原則としてCP-SATが選んだ訪問先をすべて経路化する。経路化できない案は `routing_infeasible` として最終候補から除外する。固定アポイントは常に除外不可とする。探索時間に上限を設定し、最適性を証明できなくても制約を満たす実行可能解は比較対象に含める。

### 8.7 複数案の比較

Plan EvaluatorがRoutingModelの結果を比較し、7.4の優先順位に従って最終案を1つ選ぶ。

比較時にはCP-SATの事業価値だけでなく、RoutingModelが算出した実移動時間を使用する。選ばれなかった案も、事業価値、移動時間、不採用理由を監査用に保存する。

### 8.8 Qwenによる説明

Qwenへ渡すのは、FastAPIが確定した次の情報だけとする。

- 訪問順と予定時刻
- 顧客名、商談フェーズ
- 売上予定額、予定粗利、期待売上、期待粗利
- 移動時間、距離
- 選択理由、除外理由
- データ不足やAPIエラー

Qwenの回答例に含める内容：

- 1日の訪問順
- 合計売上予定額と合計予定粗利
- 合計期待売上と合計期待粗利
- 合計移動時間
- 売上を優先した訪問先
- 粗利を優先した訪問先
- 見送った主な候補と理由
- 「金額は予定値であり確定実績ではない」という注記

Qwenは数値を再計算・補完・丸め直ししない。

### 8.9 承認と保存

1. 画面に計画案を表示する。
2. ユーザーが「この計画を採用」を押す。
3. 承認時に既存予定との重複を再確認する。
4. 問題がなければ `activity_plan` に訪問予定を保存する。
5. ルート計画を `approved` に更新する。

計画作成だけでは既存予定を追加・変更・削除しない。承認失敗時は部分保存せず、トランザクションを取り消す。

## 9. API仕様

### 9.1 計画案の作成

`POST /api/route-plans/preview`

入力：

| 項目 | 必須 | 内容 |
|---|---|---|
| `target_date` | 必須 | 計画対象日 |
| `policy` | 任意 | `balanced` / `sales` / `gross_profit` / `short_travel` |
| `max_visits` | 任意 | 既定5、管理側上限以内 |
| `work_start` | 任意 | 既定09:00 |
| `work_end` | 任意 | 既定18:00 |
| `min_expected_sales` | 任意 | 1日に確保したい期待売上の最低額 |
| `min_expected_gross_profit` | 任意 | 1日に確保したい期待粗利の最低額 |

`rep_id` はリクエスト本文を信用せず、認証済みログインユーザーから取得する。

出力：

- 計画ID、状態
- 訪問順と時刻
- 各訪問の売上・粗利指標
- 区間移動時間・距離
- 合計値
- CP-SATとRoutingModelの求解状態
- 最低売上・粗利条件の達成状況と不足額
- 選定理由、除外理由
- 警告

### 9.2 計画の承認

`POST /api/route-plans/{plan_id}/approve`

- 本人の `proposed` 計画だけ承認できる。
- 承認済み計画の重複承認は冪等に扱う。
- 承認後の `activity_plan` IDを返す。

### 9.3 計画の却下

`POST /api/route-plans/{plan_id}/reject`

- 計画を `rejected` にする。
- `activity_plan` は変更しない。

### 9.4 Qwen用ツール

`create_sales_route_plan(target_date, policy, max_visits, min_expected_sales, min_expected_gross_profit)`

内部でプレビューAPI相当の処理を呼び、構造化された結果をQwenへ返す。Qwenの文章をAPI入力としてそのままSQLへ使用しない。

## 10. 画面仕様

計画案画面には最低限次を表示する。

- 対象日、担当者、所属営業所
- 地図上の出発地点、訪問地点、訪問順
- 到着・出発予定時刻
- 顧客名と商談フェーズ
- 売上予定額、予定粗利、予定粗利率
- 期待売上、期待粗利
- 最低期待売上・最低期待粗利の達成状況と不足額
- 区間移動時間、合計移動時間
- 選定理由、見送り理由
- データ品質の警告
- 「この計画を採用」「作り直す」「却下」操作

粗利情報がない案件を0円と表示せず「粗利評価不可」と表示する。

## 11. エラー処理

| 状況 | 処理 |
|---|---|
| 顧客の座標がない | 候補から除外し、対象件数を警告 |
| Geocoding精度不足 | `review` とし、自動採用しない |
| 営業所の座標がない | 計画作成を中止 |
| 担当エリア外 | 候補から除外し、理由を記録 |
| 原価がない | 粗利評価不可。売上のみで評価したことを表示 |
| Routes APIの一部失敗 | 該当候補を除外して再計算 |
| Routes API全体停止 | 計画を `failed` にし、保存しない |
| 固定予定だけで勤務時間超過 | 最適化せず警告 |
| CP-SATで必須条件を満たす案なし | 不足額を返し、条件緩和した代替案を明示 |
| RoutingModelで一部案が経路化不可 | 該当案を除外し、残りの案を比較 |
| RoutingModelですべて経路化不可 | 計画を保存せず、制約見直しを案内 |
| Qwen停止 | 構造化された計画案は画面表示可能にする |
| 承認時に予定が競合 | 保存を中止して再計画を案内 |

## 12. セキュリティと運用

- Geocoding APIキーとRoutes APIキーはFastAPIのシークレットとして管理する。
- APIキーをフロントエンド、Qwenプロンプト、レスポンス、ログへ出さない。
- Qwenには全顧客データを渡さず、選定後の候補と計画結果だけを渡す。
- 認証済み営業担当者本人のデータだけを計画対象にする。
- 外部APIへ送る住所と送信目的を運用上明確にする。
- API呼び出し回数、失敗率、料金、処理時間を記録する。
- 計画入力、計算値、使用重み、Qwenモデル、承認結果を監査ログへ残す。
- 住所変更時だけ再Geocodingし、通常処理で毎回外部APIへ送信しない。

## 13. Qwenのチューニング方針

MVPではファインチューニングと強化学習を行わない。既存のQwenに、短いシステムプロンプト、構造化ツール呼び出し、出力スキーマを組み合わせる。

Qwenに学習させるのではなく、次をログとして蓄積する。

- 提案した計画
- ユーザーが採用・却下・修正した結果
- 実際に訪問したか
- 商談結果
- 計画売上・粗利と結果の差
- 移動時間の予測と実績の差

十分な利用履歴がたまった後、候補順位の重み調整やContextual Banditを検討する。売上・粗利条件はCP-SAT、ルート制約はRoutingModelに残し、将来もLLMや強化学習だけで置き換えない。

## 14. 受け入れ条件

1. 300社すべてにGeocodingの `success`、`review`、`failed` のいずれかが記録される。
2. 住所と取得座標の都道府県が一致することを検証できる。
3. 担当エリア外の過去商談が自動候補へ入らない。
4. PostGISが近隣候補を最大20社返す。
5. 売上予定額、予定粗利、期待売上、期待粗利が同じ入力から常に同じ値になる。
6. `cost` がない場合に粗利0円として扱われない。
7. CP-SATが最低売上・最低粗利・最大訪問数などの設定条件を守る。
8. CP-SATが異なる訪問先セットを最大10案生成できる。
9. Routes APIの移動時間と距離が各案のRoutingModelへ渡される。
10. RoutingModelが固定予定、勤務時間、滞在時間、最大訪問数を破るルートを作成しない。
11. 経路化できない案が最終候補から除外される。
12. Plan Evaluatorが事業条件を満たす案を優先し、その中から粗利・売上・移動時間で最終案を決める。
13. 同じ顧客の複数商談が1回の訪問にまとめられる。
14. 計画案に合計売上予定額、合計予定粗利、合計期待売上、合計期待粗利、合計移動時間が表示される。
15. 最低条件を満たせない場合、条件を無視せず不足額と代替案が表示される。
16. Qwenの説明数値がFastAPIの返却値と一致する。
17. ユーザー承認前に `activity_plan` が変更されない。
18. APIエラー時に不完全な計画が自動保存されない。
19. 承認時の保存がトランザクション内で完了する。

## 15. 実装順序

### フェーズ1：DBと位置情報

- PostGISを有効化する。
- 顧客と営業所へ位置情報項目を追加する。
- 営業所の実在住所を登録する。
- 300社の住所を一括Geocodingする。
- Geocoding結果の確認手段を用意する。

### フェーズ2：候補と経済指標

- 担当エリア一致の候補抽出を実装する。
- PostGISで近隣候補を最大20社に絞る。
- 売上予定額、予定粗利、期待売上、期待粗利を計算する。
- データ不足時の扱いを実装する。

### フェーズ3：移動時間と最適化

- Routes APIとキャッシュを実装する。
- CP-SATへ事業条件を渡し、異なる上位10案を生成する。
- 各案をRoutingModelへ渡し、訪問順と時刻を計算する。
- `PARALLEL_CHEAPEST_INSERTION` と `GUIDED_LOCAL_SEARCH` を既定設定にする。
- Plan Evaluatorで実行可能案を比較する。
- 最終案、代替案、求解状態、除外理由を返す。

### フェーズ4：Qwenと画面

- Qwenへ `create_sales_route_plan` ツールを公開する。
- 計画案、地図、売上・粗利、移動時間を表示する。
- 承認・却下・作り直しを実装する。
- 承認後に `activity_plan` へ保存する。

### フェーズ5：検証

- 札幌、仙台、東京、中部、神戸、広島、九州の各担当者で動作確認する。
- 固定アポイントあり・なしを検証する。
- 原価なし、住所変換失敗、ルート取得失敗を検証する。
- 売上重視、粗利重視、移動重視で訪問先が適切に変わることを確認する。
- CP-SATの最高評価案が経路化できない場合、次の実行可能案が採用されることを確認する。
- 最低期待売上・最低期待粗利を満たせない場合、不足額と代替案が返ることを確認する。

## 16. 着手前に決定する項目

- 7営業所それぞれの実在住所
- Geocoding APIとRoutes APIの契約・利用上限
- 近隣検索の初期半径
- 標準勤務時間と休憩時間
- 標準訪問時間
- 1日の最大訪問数
- 売上・粗利・緊急度・移動時間の初期重み
- CP-SATが生成する代替案数（既定10）
- CP-SATとRoutingModelそれぞれの制限時間
- 必須条件を満たせない場合の条件緩和順序
- 固定アポイントを判定する既存データ条件
- `cost` を実運用でどこから取得するか
- 受注済み `estimated_amount` を確定売上として扱うか、実績項目を追加するか
- ルート計画案の保存期間

## 17. 実装担当者への注意事項

- 現在の住所はルート計画へ利用できるが、文字列のままではPostGISやOR-Toolsへ渡せない。必ず事前に座標化する。
- 粗利用の新しい金額カラムはMVPでは追加しない。既存の `estimated_amount` と `cost` から都度計算する。
- シードデータの原価はデモ値なので、計算機能の検証には使えるが経営判断の正解データとして扱わない。
- Qwenの出力を最適化結果として信用せず、数値と順番の正本はFastAPI・OR-Toolsの結果とする。
- CP-SATの1位案だけをRoutingModelへ渡して確定しない。複数案を実際の移動時間で比較する。
- CP-SATは訪問先の組み合わせ、RoutingModelは訪問順と時刻という責務を崩さない。
- GitHubのシード変更だけでは、起動中のSupabaseへ自動反映されない。マイグレーション適用と必要なデータ更新手順を別途用意する。
