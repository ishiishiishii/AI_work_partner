# AGENTS.md — AI Work Partner

このファイルは、本リポジトリで作業する AI / 人間の共通コンテキストです。実装・設計判断はこの文書に従ってください。

---

## 1. プロダクト概要

**一行定義**  
営業目標から逆算し、「誰に・いつ・どの程度アプローチするか」を根拠つきで設計し、崩れたら組み直す AI 営業計画エージェント。

**未来のニュース（ビジョン表現）**  
「目標に追われる営業」から「目標達成への道筋が見える営業」へ — AI 営業計画エージェント『AI Work Partner』提供開始

**チーム**  
Cチーム / AI Work Partner

---

## 2. WHO（誰のための製品か）

売上目標を持ち、その達成計画を自分で立てている営業社員。

- 顧客候補は提示されるが、「誰に・いつ・どの程度」は自己判断している
- 複数の顧客・案件を並行して担当し、活動を最適化する必要がある
- 得意業界・過去実績・先輩の経験やデータを活かしきれていない
- 計画づくり自体に時間と負担を感じている

**拡張先:** メンバーごとの計画と進捗を調整するチームリーダー

---

## 3. PROBLEM（解決する課題）

目標は明確なのに、計画は個人任せ。

- 目標から必要商談数を逆算し、顧客候補と予定を突き合わせる「採算取り」に手間がかかる
- 考慮すべき情報が多く、その計画で目標に届くのか本人にも判断しづらい
- 計画の立て方が経験と勘に依存し、担当者ごとにばらつく
- 前提が崩れるたびに、残目標から組み直しが発生する
- 計画作成と進捗確認に時間を取られ、商談準備や顧客対応の時間が削られる

---

## 4. SOLUTION（提供価値）

営業目標から逆算し、活動計画を設計する AI エージェント。

| 機能 | 内容 |
| --- | --- |
| 逆算による計画生成 | 月間の売上・契約目標から必要活動量を逆算し、月／週／日に落とす |
| 顧客ごとの見込み推定 | 過去実績・商談履歴・業界・属性から成約可能性と期待成果を推定 |
| 担当者特性の反映 | 「製造業に強い」等の個人傾向を計画に織り込む |
| 負担とのバランス設計 | 成約確率順ではなく、目標金額・必要活動量・移動時間を踏まえて組む |
| 根拠の提示 | 「過去実績から達成見込み ○%」など、なぜこの計画かを説明 |
| 自動再計画 | 延期・失注・新規・想定外受注に対し、残目標と残期間から組み直す |
| 事務作業の代行 | 集計、レポート作成、数字管理 |

---

## 5. EXPERIENCE

**Before**  
数字と顧客リストだけ渡され、計画は経験に頼って自力で組み立てる。予定が崩れるたびに組み直しに追われる。

**After**  
AI が根拠つきの計画を示し、崩れてもそのつど再提示する。使うほど自分に合った計画に近づき、「目標に追われる営業」から「道筋が見えて納得できる営業」へ変わる。

**Evidence**  
営業社員から「数字を立てるのがしんどい」という声

**Interview 観点**  
計画が崩れたとき、現在はどう対応しているか

---

## 6. バリュープロポジション

### Customer Jobs
- 残ノルマと期限から数字を組み立てる
- 提案書・メール・ターゲット分析資料を準備する
- 顧客候補から誰に・いつ・どの程度アプローチするか判断する

### Pains
- 営業先選定に手間（過去資料とノルマの両にらみ）
- 計画が経験と勘に依存しばらつく
- 計画作成と進捗確認に時間を取られ、本来の活動時間が減る

### Gains
- 生産性向上 / 業務削減
- 数字の根拠を提示できる
- 顧客に向き合う時間が増える

### Pain Relievers
- 顧客リストを調査せず最適な訪問先を選定
- 数字管理の手作業をなくす
- 手が回らない状況を減らす / 判断材料が揃い迷いが減る

### Gain Creators
- 「誰に・何を・どれくらいの粗利で」を自動算出して優先順位化
- 事務時間が浮く
- 提案商材と見込み利益を一気通貫で提示
- 粗利と優先度が数値的根拠になる

### Products（機能群の位置づけ）
- パーソナライズされた AI 秘書
- ターゲットリストの整理と優先順位付け
- 売上・粗利シミュレーション
- 集計・レポート代行
- アクシデント時の計画修正

---

## 7. MVP のコア体験（最優先で守るフロー）

```text
営業目標を入力する
  ↓
顧客候補・過去実績・担当者の特徴を AI が分析
  ↓
目標達成から逆算した月・週・日の営業計画を生成
  ↓
なぜその顧客・予定なのかを AI が説明
  ↓
営業担当者が計画に沿って活動
  ↓
成約・失注・延期などの結果を入力
  ↓
AI が目標達成見込みを再計算
  ↓
状況に応じて残り期間の営業計画を自動で再構築
```

**MVP として最優先する価値**  
「目標だけを与えられ、達成方法を自分で考える」状態から、  
「AI が自分の実績や強みを踏まえて、目標達成までの道筋を継続的に描き直してくれる」状態への変化。

実装・UI・API 設計で迷ったら、このコア体験を壊さない方を選ぶ。

---

## 8. MVP 機能スコープ

> 2026-08-25 時点で、Must の 1〜4・6〜10 は実装済み。5(目標からの逆算)は対応中（担当メンバーあり）。
> Should のうち「自然言語による計画変更」以外は実装済み。

### 必須機能（Must）

1. **営業目標の設定** ★☆☆☆☆ ✅ 実装済み  
   月間売上目標・契約件数などを入力し、計画生成のゴールにする。

2. **顧客候補・営業データの登録** ★★☆☆☆ ✅ 実装済み  
   顧客候補、業界、見込み売上、過去商談・成約実績など。MVP ではサンプルデータでも可。

3. **営業担当者の特徴・過去実績の登録** ★★☆☆☆ ✅ 実装済み  
   得意業界・顧客傾向を保持し、計画をパーソナライズする。

4. **顧客ごとの成約可能性・期待売上の算出** ★★★☆☆ ✅ 実装済み  
   ルールベース / 簡易スコアリングで可（複雑 ML は不要）。  
   例: 成約可能性 × 見込み売上。

5. **月間営業計画の自動生成** ★★★☆☆ 🔧 対応中（担当メンバーあり。他の項目の作業では触らない）  
   目標から逆算し、優先顧客を選定。達成可能性と負担を考慮。  
   現状は期待値（確度 × 見込み金額）順に埋めるのみで、目標必達に必要な件数からの逆算はまだ無い。

6. **週・日単位への落とし込み** ★★★☆☆ ✅ 実装済み  
   カレンダー形式などで「今週 / 今日」を提示。

7. **計画の根拠説明** ★★☆☆☆ ✅ 実装済み  
   自然言語で「なぜ A 社を優先するか」を説明。

8. **目標達成見込みの可視化** ★★☆☆☆ ✅ 実装済み  
   例: 目標 1,000 万円に対して期待売上 1,080 万円。

9. **商談結果・状況変化の入力** ★☆☆☆☆ ✅ 実装済み  
   成約 / 失注 / 延期 / 新規など。計画へ反映。

10. **状況変化に応じた自動再計画** ★★★☆☆ ✅ 実装済み  
    残期間・残目標から再構築し、変更理由も提示。

### 余裕があれば（Should / Nice）

- 得意領域の自動分析 ✅ 実装済み
- 自然言語による計画変更（「金曜は訪問を減らしたい」等）— 未実装。AIチャットは質問応答のみで、計画そのものを操作するまでは対応していない
- AI との営業計画チャット ✅ 実装済み
- 計画の手動修正（AI 提案の強制なし）✅ 実装済み

### 未実装機能（次フェーズ候補）

MVPのコア体験を優先するため後回しにしてきた機能群。Must / Should が一巡した今、着手先の候補として扱ってよい。ただし複数人で同時に手を付けると衝突しやすいので、着手前にチームに一声かけること。

- Google Calendar 連携
- CRM / SFA 連携
- 訪問ルート・移動時間の最適化
- タスク自動生成・日々の仕事管理への拡張
- 利用実績に基づく継続学習パーソナライズ
- チーム全体の担当最適化

---

## 9. データモデル方針

詳細スキーマは実装時に `supabase/migrations/` へ SQL で落とす。ここでは設計原則と主要エンティティのみ示す。

### 設計原則

- **sales_rep を軸**に `sales_target → activity_plan → activity_result` を接続する
- `deal` / `customer` の過去実績・属性と `rep_affinity` が AI 計画の参照データになる
- **複合外部キー**と **トリガー**で関連間の矛盾データ（会社と担当者の不一致、成約なのに契約日なし等）を DB レベルで防ぐ
- マスタは参照専用として正規化し、コアは更新系

### マスタ（参照）テーブル

`branch`, `department`, `industry`, `customer_rank`, `customer_status_master`, `company_size_master`, `work_style_master`, `product_category`, `deal_phase`, `deal_result_status`, `lead_source`, `loss_reason`, `deal_pattern`, `activity_type`, `plan_status_master`

要点:
- `deal_phase` は成立前の進行段階のみ（初回接触〜契約交渉）
- `deal_result_status` は進行中 / 成約 / 失注。`is_won` と `is_lost` は同時に 1 不可

### コアテーブル

| テーブル | 役割 |
| --- | --- |
| `sales_rep` | 営業担当（上長は自己参照 `manager_rep_id`） |
| `customer` | 顧客企業（主担当 `primary_rep_id`） |
| `customer_contact` | 先方キーパーソン。`(contact_id, customer_id)` UNIQUE |
| `product` / `deal_line` | 商材と商談明細 |
| `deal` | 商談。`(deal_id, customer_id)` UNIQUE。複合 FK で contact / plan / result の整合を担保 |
| `sales_target` | 月次目標 `(rep_id, target_month)` |
| `rep_affinity` | 得意分野スコア `(rep_id, category_id, pattern_id)` |
| `activity_plan` | AI 生成を含む活動計画（`is_ai_generated`） |
| `activity_result` | 活動実績（`plan_id` は NULL 可＝計画外） |

### 必須トリガー方針

`deal` INSERT/UPDATE 時:
- 成約 (`is_won=1`) → `contract_date` 必須
- 失注 (`is_lost=1`) → `loss_reason_id` 必須
- それ以外 → `contract_date` / `loss_reason_id` はともに NULL

### AI 計画ロジックへの接続（実装ガイド）

1. 入力: `sales_target` + 担当の `customer` / `deal` + `rep_affinity`
2. スコアリング: ルールベースで期待成果（例: 確度 × 見込金額）と負担（訪問回数・工数）を計算
3. 出力: `activity_plan`（月→週→日）と根拠テキスト
4. フィードバック: `activity_result` / `deal` 更新 → 達成見込み再計算 → 再計画

MVP の AI は **オープンモデル未定**。当面はルールベース + 根拠文テンプレート / LLM プレースホルダ（`backend/app/services/ai.py`）でよい。モデル差し替え可能な境界を保つこと。

---

## 10. 技術スタックとリポジトリ構成

| 層 | 技術 | 備考 |
| --- | --- | --- |
| フロント | Next.js (App Router) + TypeScript + React | `frontend/` |
| API | FastAPI (Python 3.12) | `backend/` |
| DB / Auth | ローカル Supabase（Postgres 等） | `supabase/` + ホストの Supabase CLI |
| AI | オープンモデル（未定） | スタブ経由で後差し替え |
| 実行環境 | Docker Compose（`api` / `web`） | `docker-compose.yml` |

```text
AI_work_partner/
  backend/          # FastAPI
  frontend/         # Next.js
  supabase/         # config / migrations / seed
  docker-compose.yml
  .env.example
  AGENTS.md
  CLAUDE.md
  README.md
```

---

## 11. 開発・Docker 作業上の注意事項（必読）

### 11.1 基本方針

- **アプリ（FastAPI / Next.js）の実行・依存インストール・検証は Docker コンテナ内で行う**ことを原則とする
- ホストに Node / Python を入れて動かすのは例外（緊急デバッグ等）。恒久手順にしない
- **Supabase CLI はホストにインストール**する（CLI が Docker で DB 群を起動するため）。CLI 自体をアプリコンテナに入れない

### 11.2 起動の順序

```powershell
# 1) Docker Desktop 起動済みであること
# 2) ローカル Supabase
supabase start
supabase status   # → .env に URL / anon / service_role を反映

# 3) アプリ
docker compose up --build
```

| URL | 用途 |
| --- | --- |
| http://localhost:3000 | フロント |
| http://localhost:8000/docs | FastAPI OpenAPI |
| http://127.0.0.1:55321 | Supabase API |
| http://127.0.0.1:55323 | Supabase Studio |

### 11.3 コンテナに入って作業する

ファイル編集はホストのエディタ（Cursor）で行い、**実行・パッケージ追加・マイグレーション検証はコンテナ内**で行う。

```powershell
# API コンテナ
docker compose exec api sh
# 例: pytest / python -c / pip が必要な場合は requirements.txt を更新して rebuild

# Web コンテナ
docker compose exec web sh
# 例: npm スクリプト確認。依存追加は package.json を編集後、コンテナ内で npm install
```

注意:
- `frontend/node_modules` は名前付き volume。ホストと中身が一致しないことがある → **npm 操作はコンテナ内**
- ホットリロードは volume マウント済み。コード変更はコンテナ再起動なしで反映される想定
- コンテナからホスト上の Supabase へは `host.docker.internal`（`.env` の `*_DOCKER`）を使う
- ブラウザから見える URL は常に `localhost`（`NEXT_PUBLIC_*`）。コンテナ内部名 `api` をブラウザに書かない

### 11.4 DB / マイグレーション

- スキーマ変更は `supabase/migrations/` に SQL を追加
- 適用・リセットはホストで `supabase db reset` 等（CLI 経由）
- シードは `supabase/seed.sql`。MVP はサンプル顧客・担当・目標を入れてコア体験をデモ可能にする

### 11.5 AI エージェントへの実装ルール

- Must MVP（セクション8）を優先する。5(目標からの逆算)は対応中の担当者がいるため、他の作業では触らない
- 「未実装機能」に着手する場合は、既存のコア体験（セクション7）を壊さないことを最優先し、着手前にチームへ共有する
- スコアリングは説明可能（根拠テキストを出せる）に保つ。ブラックボックス単独の ML を MVP の必須にしない
- DB 制約・トリガーで守るべき整合はアプリだけに頼らない
- 秘密情報（`.env` の key）をコミットしない
- コミットはユーザーが依頼したときのみ行う

---

## 12. 実装時の優先順（推奨）

1. Supabase マイグレーション（マスタ + コア）と seed
2. 目標・顧客・担当者の CRUD / 参照 API + 簡易 UI
3. ルールベースの期待成果算出
4. 月次計画生成 → 週日展開 → 根拠・達成見込み表示
5. 結果入力 → 再計画

迷ったらセクション 7 のコア体験に戻る。
